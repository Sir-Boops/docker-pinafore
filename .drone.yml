
pipeline:
    docker:
        image: plugins/docker
        dockerfile: Dockerfile
        repo: sirboops/pinafore
        tags: [0.18.0, latest]
        experimental: true
        squash: true
        secrets: [ docker_username, docker_password ]
